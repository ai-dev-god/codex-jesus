You are the tech lead breaking the roadmap into thin, shippable slices. Only include tasks that move the MVP toward a working local demo.

Inputs:
- docs/roadmap.md
- docs/documentation.md

Outputs:
- docs/tasks-frontend.md
- docs/tasks-backend.md

Rules:
- Every task should be completable in ≤ 0.5–2 days.
- Be explicit about files/folders touched and commands to validate.
- Include ONLY MVP-scope work. Exclude prod infra, release, compliance/security/privacy, scaling/perf tasks.
- Prefer vertical slices that end in a user-visible improvement.
- If a required script, file, or dependency is not yet in the repository, mark the relevant step as `Placeholder — to be implemented` and note which follow-up task should create it.
- Reference the DevOps tooling (docker-compose services, `./devops/*.sh` scripts, `.env` variables) established by the prior agent. Commands must match the actual files where possible; otherwise label them as placeholders with TODO owners.

Backlog format:

# Frontend Tasks
## <Milestone Name>
### Task FE-01 — <Title>
- Task ID: FE-01
- Folder: /bh-fe (and subpaths)
- Why: short context tied to a user story
- Steps: ordered list with file paths, components to create/update, and props/state to wire
- Acceptance Criteria: Given/When/Then; include visual/URL expectation if relevant
- Validation: commands to run (dev server, tests, lints) + what to look for
- Dependencies: task IDs or files
- Effort: <story points or ideal days>

...continue numbered tasks...

### Open Questions / Follow-ups
- <bullets>

# Backend Tasks
## <Milestone Name>
### Task BE-01 — <Title>
- Task ID: BE-01
- Folder: /backend (and subpaths)
- Why: short context tied to a user story
- Steps: migrations/models, endpoints (method + path), request/response shape, error cases that affect the happy path
- Acceptance Criteria: Given/When/Then; include example curl with expected JSON
- Validation: commands (seed, run, test, lint) + expected output
- Dependencies:
- Effort:

### Open Questions / Follow-ups
- <bullets>
